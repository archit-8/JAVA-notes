Collection in java DSA Question
1. Find secondmax and min element in a given list
  Input:[10,5,4,32,45]
  output secondmin=5
        Secondmax=32
       
import java.util.*;
public class Main
{
	public static void main(String[] args) {
	    
	    List<Integer> list= List.of(10, 20, 4, 45, 99);
        List<Integer> sorted= new ArrayList<>(new TreeSet<>(list));
        Integer second= sorted.get(sorted.size()-2);
        Integer secondmin=sorted.get(1);
        System.out.println(second);
        System.out.println(secondmin);
  }
}
2.Count Frequency of each word into a Sting
 Input="rabbit"
output= r=1 a=1 b=2 i= t=1
import java.util.*;
public class Main
{
	public static void main(String[] args) {
	    
	    String name="programming";
	    char [] ch= name.toCharArray();
	    
	    Map<Character,Integer> map=new  LinkedHashMap<>();
	    
	    for(Character s:ch){
	        
	        map.put(s,map.getOrDefault(s,0)+1);
	    }
        for(Map.Entry<Character,Integer> m: map.entrySet()){
            System.out.println(m.getKey()+" ="+m.getValue());
        }

keep in mind these question while sloving problem
List → add(), get(), remove(), size()
Set → add(), contains(), remove(), size()
Map → put(), get(), getOrDefault(), containsKey(), entrySet(), keySet()


3.Common element in two array list
Lis1=[1,2,3,4]
List2=[3,4,5,6]
ouput:[3,4]

import java.util.*;
public class Main
{
	public static void main(String[] args) {
	    
	  List<Integer> list1= List.of(1, 2, 3, 4,4;
	  List<Integer> list2= List.of(3, 4, 5, 6);
      List<Integer> common= new ArrayList<>();
      Iterator<Integer> first= list1.iterator();

	  
	  while(first.hasNext()){
	      Integer num=first.next();
	      if(list2.contains(num)){
	          common.add(num);
	      }
	  }
	  
	  System.out.println("common element in array"+common); 
	}
}
this complixity is o(n log n)

second option:
   import java.util.*;

public class Main {
    public static void main(String[] args) {

        List<Integer> list1 = List.of(1, 2, 3, 4);
        List<Integer> list2 = List.of(3, 4, 5, 6);

        Set<Integer> set1 = new HashSet<>(list1);
        Set<Integer> set2 = new HashSet<>(list2);

        set1.retainAll(set2); // keeps only common elements

        System.out.println("Common Elements: " + set1);
    }
}

4.sort word according to length

 import java.util.*;

class SortByLength implements Comparator<String> {
    @Override
    public int compare(String s1, String s2) {
        // Compare based on length
        if (s1.length() < s2.length())
            return -1;
        else if (s1.length() > s2.length())
            return 1;
        else
            return 0;
    }
}

public class Main {
    public static void main(String[] args) {
        List<String> words = new ArrayList<>(List.of("apple", "kiwi", "banana", "fig"));

        Collections.sort(words, new SortByLength()); // use our comparator

        System.out.println(words);
    }
}
class SortByLengthThenName implements Comparator<String> {
    @Override
    public int compare(String s1, String s2) {
        if (s1.length() != s2.length())
            return s1.length() - s2.length(); // sort by length
        else
            return s1.compareTo(s2); // if same length, sort alphabetically
    }
}


       	  
